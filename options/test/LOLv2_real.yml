# must be same as
name: test
suffix: ~  # add suffix to saved images
model: ll_dataset
distortion: sr
scale: 1
crop_border: ~  # crop border when evaluation. If None(~), crop the scale pixels
gpu_ids: []

datasets:
  test:
    name: test
    dataroot_LQ: D:/cobaa/FourLLIE-main/data_test/input
    dataroot_GT: D:/cobaa/FourLLIE-main/data_test/gt

#### network structures
network_G:
  which_model_G: FourLLIE
  nf: 16  # 32 or 64 possible if VRAM cukup

#### path
path:
  root: ./
  pretrain_model_G: ./pre-trained/nikon.pth
  # alternatif checkpoint:
  # pretrain_model_G: ./pre-trained/huawei.pth
  # pretrain_model_G: ./pre-trained/lol2-real.pth
